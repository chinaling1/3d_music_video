#include <gtest/gtest.h>

TEST(AnimationCurve, Placeholder) {
    EXPECT_TRUE(true);
}

TEST(AnimationIK, Placeholder) {
    EXPECT_TRUE(true);
}

TEST(AnimationClip, Placeholder) {
    EXPECT_TRUE(true);
}
